---
name: Release created
on:
  release:
    types:
      - created
jobs:
  released:
    uses: kare/github/.github/workflows/go-release.yaml@main
    inputs:
      go_module_path: kkn.fi/uuid
      version: ${{ github.event.release.tag_name }}
